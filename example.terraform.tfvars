// resource_prefix generates names for all resources
resource_prefix = "locust-example"

// tags are applied to all deployed resources
tags = {
  Purpose     = "Locust Example"
  Environment = "Proof of Concept"
}
